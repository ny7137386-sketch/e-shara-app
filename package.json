{
  "name": "e-shara-app",
  "version": "1.0.0",
  "private": true,
  "description": "e shara handmade marketplace and community care platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check app.js && node --check server.js && node --check db/index.js && node --check middleware/auth.js && node --check routes/auth.js && node --check routes/products.js && node --check routes/orders.js && node --check routes/admin.js && node --check routes/clinics.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "razorpay": "^2.9.8"
  }
}
